import '../style.css';
import('preline')
import { createApp } from 'vue';

  const app = {
    data() {
      return {
        text: 'Weight2View'
      }
    },
    methods: {}
  };

createApp(app).mount('#app');